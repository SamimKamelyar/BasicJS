// while loop

// Initialize value e.g. let i = 1
// while ( condition/expression ) {
// code will run while the codition is true

// after the iteration we should increment the initializer
// e.g. i++
// }

//using while loop print from 100 to 0
let i = 100;
while (i >= 0) {
  console.log(i);
  i--;
};
